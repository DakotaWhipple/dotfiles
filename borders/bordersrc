#!/usr/bin/env bash
# JankyBorders — focused-window outline, colored by the active vibe.
source "$HOME/.config/theme/colors.sh"

options=(
  style=round
  width=6.0
  hidpi=on
  active_color="0xff$ACCENT"
  inactive_color="0xcc$BG_HL"
)

borders "${options[@]}"
